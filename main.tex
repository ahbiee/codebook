% Team Note Sample Template
% These codes should be guaranteed, fast enough, short and easy to type.

\documentclass[portrait, 8pt, a4paper, oneside, twocolumn]{extarticle}
\usepackage{teamnote}

% 引入中文字體處理套件
\usepackage{fontspec}
\usepackage{xeCJK}
% 讓 LaTeX 自動判斷環境中存在的字體名稱
\IfFontExistsTF{Noto Sans CJK TC}{
    \setCJKmainfont{Noto Sans CJK TC}  % 給 GitHub Actions (Linux) 用的
}{
    \setCJKmainfont{Noto Sans TC}      % 給本機 Windows 用的
}

\teamnote{FJCU}{UltraGrammer}{ahbiee}

\ShowUsage
\ShowComplexity
\HideAuthor

\begin{document}

\maketitlepage

% Make Pagebreak if you want.
% \pagebreak 

\section{你是否有...}

\subsection{嘗試...}

\begin{itemize}
    \item \textcolor{red}{\textbf{再讀一遍題目}}
    \item 懷疑你認為「理所當然」的直覺?
    \item 試著寫下直覺的規律? (有助於觀察)
    \item 換個方向思考?
    \item 暴力解?
    \item 貪心解?
    \item 對答案二分搜?
    \item 動態規劃(DP)?
    \item 狀態建模, 把問題轉化成圖論?
    \item 只在奇數index反轉狀態?
    \item 用乘法取代除法? ($/2 = *2^{-1}$，模逆元)
\end{itemize}

\subsection{檢查...}

\begin{itemize}
    \item \textcolor{red}{\textbf{確定沒漏看敘述?}}
    \item 確認數值範圍? (溢位, 精度位數, 陣列大小...)
    \item 不同的陣列/字串 使用同個index導致超出範圍?
    \item 模板抄錯?
    \item 多筆資料之間初始化?
    \item 題目額外的敘述, 暗示?
    \item 未定義行為? (陣列越界, 未初始化)
    \item overflow? (int -> long long)
    \item 非void function忘記return?
    \item 浮點數誤差? (極小的eps, 是否需要long double)
    \item 隱轉換? (int*int爆了後才存到long long)
    \item signed與unsigned int之間的compare?
\end{itemize}

\section{資料結構}

\Algorithm{BIT (Fenwick Tree)}{支援單點修改與區間求和。必須是1-based index。}{$\mathcal{O}(\log n)$}{cpp}{source/DataStructure/BIT.cpp}{}

\Algorithm{Segment Tree 線段樹}{給定n筆資料查詢與m次範圍更新，解決多次範圍更新的問題，找區間和或最大/小值。必須是1-based index。}{建樹$\mathcal{O}(N)$ 查詢/更新 $\mathcal{O}(\log N)$}{cpp}{source/DataStructure/SegmentTree.cpp}{}

\section{數論: 尤拉公式, 數論分塊, Sprague-Grundy函數, 排容原理}

\Algorithm{快速冪}{計算 $a^b \pmod m$。}{$\mathcal{O}(\log b)$}{cpp}{source/Math/FastPower.cpp}{}

\Algorithm{模逆元與費馬小定理}{定義： 若 $p$ 為質數，且整數 $a$ 與 $p$ 互質，則 $a^{p-1} \equiv 1 \pmod p$。邏輯應用： 兩邊同除以 $a$ 可得 $a^{p-2} \equiv a^{-1} \pmod p$。\\這代表計算 $(x / a) \pmod p$ 等同於計算 $(x \cdot a^{p-2}) \pmod p$。}{$\mathcal{O}(\log p)$}{cpp}{source/Math/invModulo.cpp}{}

\Algorithm{質數檢查}{單次詢問大數用 \texttt{isPrime}；若有大量詢問則建質數表。}{$\mathcal{O}(\sqrt{N})$}{cpp}{source/Math/Prime.cpp}{}

\Algorithm{大數乘法}{未知長度的大數相乘}{$\mathcal{O}(N \times M)$}{cpp}{source/Math/BigNumMultiply.cpp}{}

\section{字串}

\Algorithm{KMP 字串匹配}{尋找 \texttt{pattern} 出現位置。\texttt{pi} 陣列可求最小循環節。必須是0-based index。}
{$\mathcal{O}(N + M)$}{cpp}{source/String/KMP.cpp}{}

\Algorithm{Z-Algorithm 字串匹配}{\texttt{z[i]} 代表 $S$ 與 $S[i \dots N-1]$ 的最長共同前綴。必須是0-based index。}{$\mathcal{O}(N)$}{cpp}{source/String/Z.cpp}{}

\Algorithm{Manacher 迴文判斷}{求最長迴文子字串。預處理字串塞入 \texttt{\#} 將所有迴文轉為奇數長度。}
{$\mathcal{O}(N)$}{cpp}{source/String/Manacher.cpp}{}

\Algorithm{Trie 字典樹}{字串前綴統計或最大 XOR 問題。必須是0-based index。}{$\mathcal{O}(|S|)$}{cpp}{source/String/Trie.cpp}{}

\Algorithm{AC自動機}{多字串匹配 (Trie + KMP)，必須是0-based index。}{$\mathcal{O}(N + M)$}{cpp}{source/String/AC.cpp}{}

\section {圖論: 最大流}

需注意： \\
是否為全連通圖（connected， 只有一個component），對於連通圖要找region可以用Euler's theorem V-E+R=2 \\
undirected 無自環 \\
directed \\
weighted 有權重（點或邊）\\
multigraph 兩點之間有多條直接的路徑 \\
有向圖的outdegree, indegree，無向圖的degree \\

表示圖的方式：Adjacency matrix, adjacency list


\Algorithm{BFS與拓樸排序}{無權圖最短路徑 / 拓樸排序計算In-degree尋找相關順序，可判斷是否有環。}{$\mathcal{O}(V + E)$}{cpp}{source/Graph/BFSTopo.cpp}{}

\Algorithm{DFS}{用遞迴走訪。}{$\mathcal{O}(V + E)$}{cpp}{source/Graph/DFS.cpp}{}

\Algorithm{Dijkstra 單源最短路徑}
{找單個起點的最短路徑，不支援負權邊，支援自環。}
{$\mathcal{O}((V+E)\log V)$}{cpp}{source/Graph/Dijkstra.cpp}{}

\Algorithm{SPFA 單源最短路徑}
{也是找單個起點的最短路徑，但支援負權邊。只在"可能有負邊"或"必須檢查負環"時才用}
{預期 $\mathcal{O}(V+E)$，最差 $\mathcal{O}(VE)$}
{cpp}{source/Graph/SPFA.cpp}
{}

\Algorithm{Floyd-Warshall 多源最短路徑}
{求任意兩點之間距離。利用滾動陣列降維，\texttt{k} 必須在最外層迴圈。注意避開 \texttt{INF} 加上負權邊的陷阱。}
{時間 $\mathcal{O}(V^3)$，空間 $\mathcal{O}(V^2)$}
{cpp}{source/Graph/Floyd.cpp}
{}

\Algorithm{二分圖判定 (著色問題)}
{規定相鄰節點必須塗上不同顏色，如果在著色過程中，發現相鄰節點已經被塗上相同的顏色，則代表此圖包含奇數環Odd Cycle，不是二分圖。已考量到不連通圖的情況}
{$\mathcal{O}(V + E)$}
{cpp}{source/Graph/BipartiteCheck.cpp}
{}

\Algorithm{割點與雙連通分量}
{處理網路穩定性、單點故障分析。透過 DFS 一次遍歷找出所有的割點以及 v-BCC。由於一個割點可能同時屬於多個 BCC，我們利用 stack 記錄 DFS 走過的點，當發現割點條件成立時，再把點從 stack 彈出形成 BCC。}
{$\mathcal{O}(V + E)$}
{cpp}{source/Graph/BCC.cpp}
{}

\Algorithm{匈牙利演算法}
{無權二分圖最大匹配，透過回溯尋找增廣路徑，以達到最大匹配。分為左(S)、右(T)兩側，以及left陣列紀錄右側所選的對象}
{$\mathcal{O}(V \cdot E)$}
{cpp}{source/Graph/Hungarian.cpp}
{}

\Algorithm{KM演算法}
{適用於左右節點數皆為 $N$ 的帶權完全二分圖}
{$\mathcal{O}(N^3)$}
{cpp}{source/Graph/KM.cpp}
{}

\section{幾何}

\Algorithm{點與向量基礎 (Point \& Vector)}
{幾何基礎，定義struct與operator。包含浮點數誤差處理 \texttt{sign}，以及核心工具 \texttt{dot} (內積) 與 \texttt{cross} (外積)。}
{$\mathcal{O}(1)$}
{cpp}{source/Geometry/point.cpp}
{}

\Algorithm{判斷線段、矩形相交 (Segment Intersection)}
{利用外積進行跨立實驗。避開斜率除以零的問題，可處理端點相切與共線重疊。}
{$\mathcal{O}(1)$}
{cpp}{source/Geometry/Intersection.cpp}
{}

\Algorithm{點與線段關係 (Point \& Line)}
{判斷 1.點是否在線段上 2.求點到直線/線段最短距離。利用內外積避免解方程式與浮點數除以零。}
{$\mathcal{O}(1)$}
{cpp}{source/Geometry/PointLine.cpp}
{}

\Algorithm{任意多邊形面積}
{利用外積求多邊形面積(鞋帶公式)。頂點必須依順時針或逆時針順序給定。支援凹凸多邊形。}
{$\mathcal{O}(N)$}
{cpp}{source/Geometry/PolygonArea.cpp}
{}

\Algorithm{凸包 Convex Hull}
{求包圍所有點的最小凸多邊形。先排 $X$ 再排 $Y$。由左至右建下半部，由右至左建上半部。利用外積判斷並踢除右轉點。}
{$\mathcal{O}(N \log N)$}
{cpp}{source/Geometry/ConvexHull.cpp}
{}
 
\section{樹論}

\Algorithm{Disjoint Set}{解決集合、快速合併的問題。優化必須路徑壓縮，可選小掛大}{$\mathcal{O}(1)$}{cpp}{source/Tree/DisjointSet.cpp}{}

\Algorithm{Kruskal 最小生成樹}{需搭配並查集使用。將所有邊排序後，利用併查集檢查是否形成環。}{$\mathcal{O}(E \log E)$}{cpp}{source/Tree/Kruskal.cpp}{}

\section{DP: 分錢幣問題, Kadane's algo找最大子序列問題}

DP通用解題四步驟: \\
1. 定義陣列意義：明確寫出 dp[i] (或dp[i][j]) 代表什麼。 \\
2. 找出遞迴關係式：推導出當前狀態如何由過去狀態轉移而來。 \\ 
3. 設定邊界條件：初始化起點或基礎 dp 值（如： dp[0] = 0 ）。 \\ 
4. 確認遍歷順序：決定迴圈是從前到後（Bottom-Up）還是後到前 (Top-Down) 計算。(通常是Bottom-Up，思考 "我是如何到這一步的"去推) \\

\Algorithm{01 Knapsack 背包}{解決物品不可拆分的情況，若可拆分用Greedy拿比例最佳的部分}{$\mathcal{O}(N)$}{cpp}{source/DP/01.cpp}{}

\Algorithm{LCS 最長共同子序列(Sequence)}{二維DP與Backtrack找原始子序列}{$\mathcal{O}(NM)$}{cpp}{source/DP/LCS.cpp}{}

\Algorithm{區間DP概念 (最長迴文子序列)}{區間 DP是由字串的內部向外擴張，利用已經算好的較短子字串，來推導出較長子字串的答案。}{$\mathcal{O}(N^2)$}{cpp}{source/DP/Interval.cpp}{}

\Algorithm{跳格子}{給定一組陣列，固定跳躍距離，問你最少需要跳幾次，以及所跳過的格子index。如果數值達到long long，INF需設為1e18或0x3f3f3f3f3f3f3f3f}{$\mathcal{O}(N \times K)$}{cpp}{source/DP/jump.cpp}{}

\Algorithm{LIS / LDS 最長遞增/遞減子序列}
{嚴格遞增用 \texttt{lower\_bound}，非嚴格改用 \texttt{upper\_bound}。替換策略下的 \texttt{dp} 陣列長度正確，但內容不一定是真實子序列。}
{$\mathcal{O}(N \log N)$}
{cpp}{source/DP/LIS_LDS.cpp}
{}

\section{Greedy}

\Algorithm{一維區間全覆蓋}{需要先把輸入轉化成一個包含l, r的struct，自訂operator<，起始位置由小到大排序}{$\mathcal{O}(N \log N)$}{cpp}{source/Greedy/1Dcoverage.cpp}{}

\Algorithm{間格跳躍}{給定一組陣列，距離為index的值，問你最少需要跳多少次才能到終點}{$\mathcal{O}(N)$}{cpp}{source/Greedy/jump.cpp}{}

\Algorithm{最大不重疊區間}{對一組任務做排程，請問最多可以移除多少個任務 -> 最早結束的先選。}{$\mathcal{O}(N \log N)$}{cpp}{source/Greedy/Nonoverlapping.cpp}{}

\Algorithm{最小覆蓋區間}{給定一組陣列，包含開始時間與結束時間，求最少需要幾個房間就能完成所有任務}{$\mathcal{O}(N \log N)$}{cpp}{source/Greedy/room.cpp}{}

\Algorithm{區間選點問題}{將區間依照右界由小到大排序。遍歷所有區段，如果當前區間的左界大於目前選定點的右界，代表必須多選一個點}{$\mathcal{O}(N \log N)$}{cpp}{source/Greedy/IntervalPointSelection.cpp}{}

\Algorithm{最小化最大延遲問題}{按照到期時間從早到晚處理。記錄當下累積的耗時，並與該任務的死線相減，取最大的延遲時間}{$\mathcal{O}(N \log N)$}{cpp}{source/Greedy/job.cpp}{}

\Algorithm{Huffman樹}{每次合併兩堆，成本是兩堆的重量和，求最小總成本}{$\mathcal{O}(N \log N)$}{cpp}{source/Greedy/huffman.cpp}{}

\Algorithm{任務調度問題}{依照懲罰由大到小排序，每項工作依序嘗試可不可以放在 Di−Ti+1,Di−Ti,...,1,0，如果有空閒就放進去，否則延後執行。}{$\mathcal{O}(N)$}{cpp}{source/Greedy/JobWithDeadline.cpp}{}

\Algorithm{多機調度問題}{將工作依耗時「由大到小」排序 。每次將工作交給最快空閒出來的機器。}{$\mathcal{O}(N \log N)$}{cpp}{source/Greedy/ProcessorScheduling.cpp}{}

\section{MISC}

\Algorithm{模板}{競賽用基礎模板}{$\mathcal{O}(How FastYouType)$}{cpp}{source/Others/Template.cpp}{}

\Algorithm{一些概念}{想不到怎麼解或優化的時候可以參考這些想法}{$\mathcal{咕咕嘎嘎}$}{cpp}{source/Others/Concepts.cpp}{}

\Algorithm{C++函式庫}{好用的東西可以參考}{$NULL$}{cpp}{source/Others/functions.cpp}{}

\end{document}
